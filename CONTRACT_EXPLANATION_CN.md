# AttentionLive 合约详解（中文）

## 📚 目录

1. [系统架构](#系统架构)
2. [AttentionToken 合约](#attentiontoken-合约)
3. [StreamerStakingPool 合约](#streamerstakingpool-合约)
4. [ViewerRewardPool 合约](#viewerrewardpool-合约)
5. [完整流程示例](#完整流程示例)
6. [关键概念解释](#关键概念解释)

---

## 系统架构

AttentionLive 由三个核心智能合约组成：

```
┌─────────────────────────────────────────────────────────┐
│                    AttentionLive 系统                    │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────┐                                   │
│  │ AttentionToken   │  ← ERC20 代币（ATT）              │
│  │  (ATT 代币)      │                                   │
│  └────────┬─────────┘                                   │
│           │                                              │
│           ├──────────┬──────────────────────┐           │
│           │          │                      │           │
│  ┌────────▼─────┐  ┌─▼──────────────┐  ┌───▼────────┐  │
│  │ Streamer     │  │ Viewer         │  │ Platform   │  │
│  │ Staking Pool │  │ Reward Pool    │  │ Fee        │  │
│  │ (主播质押池)  │  │ (观众奖励池)    │  │ (平台费)   │  │
│  └──────────────┘  └────────────────┘  └────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 合约关系

- **AttentionToken**: 平台的 ERC20 代币
- **StreamerStakingPool**: 主播质押 ATT 创建直播任务，获得奖励
- **ViewerRewardPool**: 观众通过观看直播获得积分，兑换 ATT

---

## AttentionToken 合约

### 📝 合约说明

这是一个标准的 ERC20 代币合约，代币符号为 **ATT**。

### 🔑 关键特性

1. **初始供应量**: 1 亿 ATT（100,000,000）
2. **小数位数**: 18 位（标准 ERC20）
3. **铸造权限**: 仅合约所有者可以铸造新代币
4. **销毁功能**: 任何人都可以销毁自己的代币

### 💡 核心函数

#### `constructor()`
```solidity
constructor() ERC20("Attention Token", "ATT") Ownable(msg.sender) {
    _mint(msg.sender, 100_000_000 * 10**18);
}
```
**作用**: 部署合约时，铸造 1 亿 ATT 给部署者

**为什么是 `10**18`?**
- ERC20 标准使用 18 位小数
- 1 ATT = 1,000,000,000,000,000,000 wei
- 类似于 1 ETH = 10^18 wei

#### `mint(address to, uint256 amount)`
```solidity
function mint(address to, uint256 amount) external onlyOwner {
    require(to != address(0), "ATT: mint to zero address");
    _mint(to, amount);
    emit Minted(to, amount);
}
```
**作用**: 铸造新代币（仅限所有者）

**使用场景**:
- 增发代币用于奖励
- 补充奖励池

#### `burn(uint256 amount)`
```solidity
function burn(uint256 amount) external {
    _burn(msg.sender, amount);
    emit Burned(msg.sender, amount);
}
```
**作用**: 销毁调用者的代币

**使用场景**:
- 减少流通供应
- 通缩机制

---

## StreamerStakingPool 合约

### 📝 合约说明

主播质押池是系统的核心合约，主播通过质押 ATT 代币创建直播任务，根据观众互动获得奖励。

### 🎯 工作流程

```
1. 主播质押 ATT → 创建任务
         ↓
2. 直播进行中 → 观众观看
         ↓
3. 后端更新观众数据
         ↓
4. 直播结束 → 计算奖励
         ↓
5. 主播领取奖励（扣除 5% 平台费）
         ↓
6. 等待冷却期（10 秒）
         ↓
7. 取回质押的 ATT
```

### 📊 数据结构

#### TaskStatus（任务状态）
```solidity
enum TaskStatus {
    Active,      // 0: 直播进行中
    Ended,       // 1: 直播已结束，可领取奖励
    Claimed,     // 2: 奖励已领取，等待冷却期
    Unstaked     // 3: 质押已取回
}
```

#### StreamingTask（直播任务）
```solidity
struct StreamingTask {
    address streamer;           // 主播地址
    uint256 stakedAmount;       // 质押的 ATT 数量
    uint256 startTime;          // 任务开始时间戳
    uint256 endTime;            // 任务结束时间戳
    uint256 duration;           // 直播持续时间（秒）
    uint256 rewardRate;         // 奖励率（基点）
    uint256 totalViewers;       // 总观众数
    uint256 totalPoints;        // 分配给观众的总积分
    uint256 streamerReward;     // 计算出的主播奖励
    TaskStatus status;          // 当前任务状态
    uint256 unstakeTime;        // 可以取回质押的时间
}
```

### 💡 核心函数

#### 1. `createStreamingTask()` - 创建任务

```solidity
function createStreamingTask(
    uint256 stakedAmount,  // 质押金额（如 10000 ATT）
    uint256 duration,      // 持续时间（如 10 秒）
    uint256 rewardRate     // 奖励率（如 500 = 5%）
) external nonReentrant returns (uint256 taskId)
```

**执行步骤**:
1. 验证参数（金额、时间、奖励率）
2. 生成新任务 ID
3. 创建任务结构体
4. 从主播账户转移 ATT 到合约
5. 触发 `TaskCreated` 事件

**示例**:
```javascript
// 质押 10000 ATT，持续 10 秒，奖励率 5%
createStreamingTask(
  10000 * 10**18,  // 10000 ATT
  10,              // 10 秒
  500              // 5% (500 基点)
)
```

#### 2. `updateViewerData()` - 更新观众数据

```solidity
function updateViewerData(
    uint256 taskId,        // 任务 ID
    uint256 totalViewers,  // 总观众数
    uint256 totalPoints    // 总积分
) external onlyOwner
```

**作用**: 由后端或预言机调用，更新观众互动数据

**为什么需要这个函数?**
- 观众数据存储在链下（数据库）
- 需要可信的数据源（Owner）将数据上链
- 用于计算主播奖励

#### 3. `endStreamingTask()` - 结束任务

```solidity
function endStreamingTask(uint256 taskId) external
```

**奖励计算公式**:
```
基础奖励 = 质押金额 × 奖励率 ÷ 10000
最终奖励 = 基础奖励 × 观众数 ÷ 100
奖励上限 = 质押金额 × 50%
```

**示例计算**:
```
质押金额: 10000 ATT
奖励率: 500 (5%)
观众数: 100

基础奖励 = 10000 × 500 ÷ 10000 = 500 ATT
最终奖励 = 500 × 100 ÷ 100 = 500 ATT
```

#### 4. `claimStreamerReward()` - 领取奖励

```solidity
function claimStreamerReward(uint256 taskId) external nonReentrant
```

**执行步骤**:
1. 验证调用者是主播
2. 验证任务状态为 `Ended`
3. 计算平台手续费（5%）
4. 转账净奖励给主播
5. 转账手续费给平台
6. 设置冷却期（10 秒后可取回质押）

**示例**:
```
奖励: 500 ATT
平台费: 500 × 5% = 25 ATT
净奖励: 500 - 25 = 475 ATT
```

#### 5. `unstake()` - 取回质押

```solidity
function unstake(uint256 taskId) external nonReentrant
```

**条件**:
- 必须已领取奖励
- 必须过了冷却期（10 秒）

**执行步骤**:
1. 验证冷却期已过
2. 转账质押的 ATT 回主播账户
3. 更新任务状态为 `Unstaked`

### 🔐 安全机制

1. **ReentrancyGuard**: 防止重入攻击
2. **SafeERC20**: 安全的代币转账
3. **Ownable**: 权限管理
4. **参数验证**: 严格的输入检查

---

## ViewerRewardPool 合约

### 📝 合约说明

观众奖励池管理观众的积分和 ATT 兑换。

### 🎯 工作流程

```
1. 观众观看直播
         ↓
2. 后端记录互动，分配积分
         ↓
3. Owner 调用 addPoints() 上链
         ↓
4. 观众积分达到 1000
         ↓
5. 观众调用 claimReward() 兑换 ATT
```

### 💡 核心概念

**兑换比例**: 1000 积分 = 1 ATT

**最低兑换**: 1000 积分

---

## 完整流程示例

### 场景：主播小明创建直播任务

#### 步骤 1: 授权 ATT
```javascript
// 前端调用
ATT.approve(StakingPoolAddress, 10000 * 10**18)
```
**作用**: 允许质押池合约使用小明的 10000 ATT

#### 步骤 2: 创建任务
```javascript
// 前端调用
StakingPool.createStreamingTask(
  10000 * 10**18,  // 质押 10000 ATT
  10,              // 持续 10 秒
  500              // 奖励率 5%
)
```
**结果**: 
- 任务 ID = 1
- 状态 = Active
- 10000 ATT 从小明账户转到合约

#### 步骤 3: 直播进行中
- 观众小红、小蓝进入直播间
- 后端记录观众数 = 100
- 后端记录互动积分 = 5000

#### 步骤 4: 更新观众数据
```javascript
// 后端调用（Owner 权限）
StakingPool.updateViewerData(1, 100, 5000)
```

#### 步骤 5: 等待 10 秒后结束任务
```javascript
// 小明调用
StakingPool.endStreamingTask(1)
```
**计算奖励**:
```
基础奖励 = 10000 × 500 ÷ 10000 = 500 ATT
最终奖励 = 500 × 100 ÷ 100 = 500 ATT
```

#### 步骤 6: 领取奖励
```javascript
// 小明调用
StakingPool.claimStreamerReward(1)
```
**结果**:
```
奖励: 500 ATT
平台费: 25 ATT (5%)
小明收到: 475 ATT
```

#### 步骤 7: 等待 10 秒后取回质押
```javascript
// 小明调用
StakingPool.unstake(1)
```
**结果**: 小明收到 10000 ATT

### 总结
```
小明投入: 10000 ATT
小明收到: 10000 + 475 = 10475 ATT
净收益: 475 ATT (4.75%)
```

---

## 关键概念解释

### 1. 基点（Basis Points）

**定义**: 1 基点 = 0.01%

**换算表**:
```
100 基点 = 1%
500 基点 = 5%
1000 基点 = 10%
10000 基点 = 100%
```

**为什么使用基点?**
- 避免小数运算
- Solidity 不支持浮点数
- 更精确的百分比表示

### 2. Wei 和 Ether

**定义**: 1 ETH = 10^18 wei

**ATT 代币**:
```
1 ATT = 1,000,000,000,000,000,000 wei
1 ATT = 10^18 wei
```

**示例**:
```solidity
// 10000 ATT
uint256 amount = 10000 * 10**18;
// = 10000000000000000000000 wei
```

### 3. 重入攻击（Reentrancy Attack）

**什么是重入攻击?**
- 恶意合约在接收代币时再次调用原合约
- 可能导致重复提取资金

**防护措施**:
```solidity
function claimReward() external nonReentrant {
    // nonReentrant 修饰符防止重入
    // ...
}
```

### 4. SafeERC20

**为什么需要 SafeERC20?**
- 某些 ERC20 代币不遵循标准
- 可能不返回 bool 值
- SafeERC20 确保安全转账

**使用方式**:
```solidity
using SafeERC20 for IERC20;

// 安全转账
attToken.safeTransfer(to, amount);
// 安全授权转账
attToken.safeTransferFrom(from, to, amount);
```

### 5. 时间戳（Timestamp）

**Solidity 中的时间**:
```solidity
block.timestamp  // 当前区块时间戳（秒）
```

**示例**:
```solidity
// 设置 10 秒后的时间
uint256 futureTime = block.timestamp + 10;

// 检查是否已过期
require(block.timestamp >= futureTime, "Not yet");
```

---

## 🎓 学习建议

### 初学者
1. 先理解 ERC20 代币标准
2. 学习 Solidity 基础语法
3. 理解合约的状态变量和函数

### 进阶
1. 学习 OpenZeppelin 库的使用
2. 理解安全模式（重入防护、权限管理）
3. 学习事件（Event）的作用

### 实践
1. 在本地测试网部署合约
2. 使用 Hardhat/Foundry 编写测试
3. 尝试修改参数并观察结果

---

## 📚 参考资源

- [Solidity 官方文档](https://docs.soliditylang.org/)
- [OpenZeppelin 合约库](https://docs.openzeppelin.com/contracts/)
- [ERC20 标准](https://eips.ethereum.org/EIPS/eip-20)
- [以太坊开发文档](https://ethereum.org/zh/developers/docs/)

---

## ❓ 常见问题

### Q1: 为什么需要先 approve 再 createTask?
**A**: ERC20 标准要求两步操作：
1. 用户授权合约使用代币
2. 合约从用户账户转移代币

### Q2: 冷却期的作用是什么?
**A**: 
- 防止快速套利
- 给平台时间处理异常
- 增加系统稳定性

### Q3: 为什么观众数据需要 Owner 更新?
**A**:
- 观众互动数据存储在链下（节省 gas）
- 需要可信数据源上链
- 未来可以使用预言机（Chainlink）自动化

### Q4: 如何防止主播作弊?
**A**:
- 后端验证观众真实性
- 限制奖励上限（50%）
- 平台审核机制

---

## 🔧 测试配置说明

当前合约使用**测试配置**，方便快速测试：

```solidity
// 测试配置
uint256 public minStakeAmount = 1000 * 10**18;  // 最低 1000 ATT
uint256 public unstakeCooldown = 10;            // 10 秒冷却期
require(duration >= 10, "Pool: min 10 seconds"); // 最短 10 秒
```

**生产环境建议**:
```solidity
// 生产配置
uint256 public minStakeAmount = 10000 * 10**18;  // 最低 10000 ATT
uint256 public unstakeCooldown = 7 days;         // 7 天冷却期
require(duration >= 300, "Pool: min 5 minutes"); // 最短 5 分钟
```

---

希望这份文档能帮助你理解 AttentionLive 的智能合约！🎉
